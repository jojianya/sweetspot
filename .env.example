# Copy this file to .env and fill in real, unique values.
POSTGRES_PASSWORD=
REDIS_PASSWORD=
JWT_SECRET=
# Public MapTiler API key, inlined into the client bundle at build time.
NEXT_PUBLIC_MAPTILER_API_KEY=
# Optional; defaults to http://localhost:8081.
NEXT_PUBLIC_API_URL=