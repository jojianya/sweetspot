**Priority:** P0-critical

From the full code review — see [`CODE_REVIEW.md#p0.4`](https://github.com/jojianya/sweetspot/blob/development/docs/CODE_REVIEW.md).

---

### Fix

- [ ] **Validate password length in bytes, not runes** — `server/internal/modules/auth/dto.go` (`binding:"min=8,max=72"`) + `server/pkg/password/password.go`
- [ ] Use `utf8.RuneCountInString(p) >= 8 && len(p) <= 72`
- [ ] Or pre-hash with SHA-256 before bcrypt (the standard fix) and keep the 72-byte rule

### Context

`go-playground/validator` measures strings in **runes**; bcrypt truncates at **72 bytes**. A 72-rune
password of non-ASCII (e.g. `"é".repeat(72)` = 144 bytes) passes validation and is silently cut to its
first 72 bytes. Users set a password, and part of it becomes the actual credential.
## P1 — High

---

- [ ] Verified locally (`go vet ./...` / `tsc --noEmit` / `vitest run` as applicable)
- [ ] Regression test added where the fix is testable
- [ ] Lint + typecheck still clean

<sub>Filed automatically from the code review. See [`CODE_REVIEW.md`](https://github.com/jojianya/sweetspot/blob/development/docs/CODE_REVIEW.md) for the full prioritized list.</sub>
