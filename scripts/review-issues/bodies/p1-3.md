**Priority:** P1-high

From the full code review — see [`CODE_REVIEW.md#p1.3`](https://github.com/jojianya/sweetspot/blob/development/CODE_REVIEW.md).

---

### Fix

- [ ] **Switch to the documented bbox predicate** — `server/internal/modules/pins/repository.go:217` and `:259` Functionally correct (distance from an interior point to the containing polygon is 0) but nobody reading it will know that, and PostGIS's geography distance path with distance 0 will not always pick the GiST index.
- [ ] Replace with `AND ST_Intersects(p.location, ST_MakeEnvelope($1,$2,$3,$4,4326)::geography)` — same semantics, obviously correct, reliably index-backed

---

- [ ] Verified locally (`go vet ./...` / `tsc --noEmit` / `vitest run` as applicable)
- [ ] Regression test added where the fix is testable
- [ ] Lint + typecheck still clean

<sub>Filed automatically from the code review. See [`CODE_REVIEW.md`](https://github.com/jojianya/sweetspot/blob/development/CODE_REVIEW.md) for the full prioritized list.</sub>
