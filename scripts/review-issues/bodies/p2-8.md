**Priority:** P2-medium

From the full code review — see [`CODE_REVIEW.md#p2.8`](https://github.com/jojianya/sweetspot/blob/development/CODE_REVIEW.md).

---

### Fix

- [ ] **Use `PanelSheet` like every other modal** — `client/src/components/pins/CreatePinButton.tsx:72-73` adds its own `window` keydown listener for Escape, while `PinEditSheet` / `ReportSheet` / `AddToCollectionSheet` all go through `PanelSheet` + `useDialogFocus`. It therefore has no focus trap, no focus restoration on close, and no `aria-modal`.
- [ ] Switch to `PanelSheet` and delete the local listener

---

- [ ] Verified locally (`go vet ./...` / `tsc --noEmit` / `vitest run` as applicable)
- [ ] Regression test added where the fix is testable
- [ ] Lint + typecheck still clean

<sub>Filed automatically from the code review. See [`CODE_REVIEW.md`](https://github.com/jojianya/sweetspot/blob/development/CODE_REVIEW.md) for the full prioritized list.</sub>
